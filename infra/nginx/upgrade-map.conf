# Place at /etc/nginx/conf.d/upgrade-map.conf — defines $connection_upgrade
# used by the Jenkins reverse-proxy websocket headers.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
