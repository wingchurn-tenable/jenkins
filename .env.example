# Copy to .env on the target host (chmod 600, root-owned). Never commit the real .env.
APP_VERSION=0.1.0
# Add your runtime config below, e.g.:
# DATABASE_URL=postgresql://user:pass@host:5432/db
# SECRET_KEY=change-me
